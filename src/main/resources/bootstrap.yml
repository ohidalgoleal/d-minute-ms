spring:
  profiles: heroku
  application:
    name: dminutems
  cloud:
    config:
      failFast: true
      uri: ${CONFIG_SERVER_DMINUTE}
      
---
spring:
  profiles: docker
  application:
    name: dminutems
  cloud:
    config:
      failFast: true
      uri: ${CONFIG_SERVER_DMINUTE}